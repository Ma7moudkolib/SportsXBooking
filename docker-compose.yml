version: '3.8'

# ─────────────────────────────────────────────
# Base / Production compose file
# Use with: docker compose up
# ─────────────────────────────────────────────

services:
  db:
    image: mcr.microsoft.com/mssql/server:2022-latest
    environment:
      SA_PASSWORD: "${SA_PASSWORD}"
      ACCEPT_EULA: "Y"
      MSSQL_PID: "Express"
    ports:
      - "1433:1433"
    volumes:
      - sqldata:/var/opt/mssql
    healthcheck:
      test: /opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P "${SA_PASSWORD}" -Q "SELECT 1" || exit 1
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    networks:
      - sports-network

  sports-booking-api:
    image: mahmoud1242002/sportsxbooking-api:latest    # ← explicit production stage
    container_name: sports-booking-api
    ports:
      - "8080:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      Database__Server: "db"
      Database__Name: "SportsBookingDB"
      Database__User: "sa"
      Database__Password: "${SA_PASSWORD}"
      Database__TrustServerCertificate: "true"
    depends_on:
      - db      #        condition: healthcheck
    networks:
      - sports-network
  sports-booking-frontend:
    image: mahmoud1242002/sportsxbooking-frontend:latest    # ← explicit production stage
    container_name: sports-booking-frontend
    ports:
      - "80:80"
    depends_on:
      - sports-booking-api
    networks:
      - sports-network

volumes:
  sqldata:


networks:
  sports-network:
    driver: bridge